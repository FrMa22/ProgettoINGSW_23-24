<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cerchio esterno -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/>
            <stroke
                android:color="@color/colore_secondario"
                android:width="2dp"/> <!-- Imposta lo spessore del contorno desiderato -->
        </shape>
    </item>

    <!-- Icona principale -->
    <item android:drawable="@drawable/ic_logout" android:left="8dp" android:top="8dp" android:right="8dp" android:bottom="8dp"/>
</layer-list>
